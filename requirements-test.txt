# Test dependencies for brew-web
# Install with: pip install -r requirements-test.txt

# Base requirements
-r requirements.txt

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-flask-sqlalchemy>=1.0.2
pytest-mock>=3.10.0
pytest-xdist>=3.0.0  # For parallel test execution

# Test utilities
pytest-postgresql>=5.0.0  # For ephemeral PostgreSQL testing
psycopg[binary]>=3.0.0  # PostgreSQL driver with binary wheels

# Code quality tools
ruff>=0.1.0  # Ultra-fast linter (replaces flake8, isort)
mypy>=1.0.0  # Type checker
bandit>=1.7.0  # Security linter (optional)
